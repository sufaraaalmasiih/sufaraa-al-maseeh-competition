# Audience v9.6.69 header + reveal timing fix

## Fixed
- Rebuilt the header layout so it no longer looks crowded.
- Moved the logo + competition name + written slogan to the right side.
- Removed the repeated "شاشة الجمهور المباشرة" label from the visual header.
- Removed the duplicate waiting message so "لم تبدأ المسابقة بعد" is shown only in the large main area.
- Improved the header appearance across all phases.
- Fixed audience reveal timing so answers do NOT auto-appear just because teams finished early.
- Auto reveal now waits for the main answer timer to actually finish, unless the host explicitly reveals earlier.
- Fixed stage 3 flow so after the answer reveal duration finishes, the audience returns to the board view while waiting for the next question.
- Improved the small reveal countdown card ("استعدوا لكشف الإجابات") so it is more compact and visually cleaner.
